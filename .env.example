
# Spring Boot backend (see backend/README.md)
VITE_API_URL=http://192.168.31.113:8081/api/v1
#VITE_API_URL=https://kidsbackend.snssystem.com/api/v1

VITE_TENANT_SLUG=demo

# Live API only — mocks require explicit opt-in
VITE_FORCE_MOCK=false
VITE_API_FALLBACK_MOCK=false

# ── Firebase Cloud Messaging (web push) ─────────────────────────────────────
# Open the portal on https://… or http://localhost (not http://192.168.x.x).
# Each browser profile registers its own FCM device token (Chrome ≠ Safari).
#
# Firebase Console → Project settings → General → Your web app
VITE_FIREBASE_API_KEY=
VITE_FIREBASE_AUTH_DOMAIN=
VITE_FIREBASE_PROJECT_ID=
VITE_FIREBASE_STORAGE_BUCKET=
VITE_FIREBASE_MESSAGING_SENDER_ID=
VITE_FIREBASE_APP_ID=
VITE_FIREBASE_MEASUREMENT_ID=
VITE_FIREBASE_VAPID_KEY=

